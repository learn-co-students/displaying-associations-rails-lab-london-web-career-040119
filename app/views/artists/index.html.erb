<% @artists.each do |artist| %>
  <h2><%= artist.name %> - <%= artist.song_count %> <%= 'song'.pluralize artist.song_count %></h2>
  <ul>
    <% artist.songs.each do |song| %>
      <li>
        <%= song.title %>
      </li>
    <% end %>
  </ul>
<% end %>


